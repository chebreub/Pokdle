name: Game regression tests

on:
  pull_request:
    paths:
      - "App/**"
      - ".github/workflows/game-tests.yml"

permissions:
  contents: read

jobs:
  game-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: App
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: App/package-lock.json
      - run: npm ci
      - run: npm run build
      - run: npm test
